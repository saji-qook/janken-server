# 念動戦 オンライン対戦サーバ

「能力じゃんけん（念動戦）」の対人対戦・連勝ランキング用 socket.io サーバ。
Render などに Docker でデプロイして使う。クライアント（saji 側）は
VITE_SERVER_URL にこのサーバの https URL を指定してビルドする。

- 待受ポートは環境変数 PORT（未指定なら 8787）。
- 連勝ランキングは ranking.json に保存（無料ホストでは再デプロイで消える場合あり）。
